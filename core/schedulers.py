# from core.handlers.play import play_scheduler


def scheduler_add(scheduler):
    # scheduler.add_job(play_scheduler, trigger='cron', minute='*', kwargs={'bot': bot})
    # scheduler.add_job(play_scheduler, trigger='cron', minute='*', kwargs={'bot': bot})
    return scheduler
